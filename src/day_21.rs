use std::fmt::Display;
use anyhow::Result;

crate::decl_tests! {}

fn eval_pt_1(input: &str) -> Result<impl Display> {
	Ok("todo")
}

fn eval_pt_2(input: &str) -> Result<impl Display> {
	Ok("todo")
}
